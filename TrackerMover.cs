using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrackerMover : MonoBehaviour {


    public GameObject tracker;
	
	// Update is called once per frame
	void Update () {




        //transform.localPosition = new Vector3(transform.localPosition.x, Mathf.Clamp(transform.localPosition.y, -10f, 0f), transform.localPosition.z);
        //if (cam.transform.eulerAngles.x < 0)
       // {
          //  transform.localPosition = new Vector3(0f, 0f, 0f);
        //}
    }
}
